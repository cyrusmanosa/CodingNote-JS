const wrapperExplorer = require('./wrapper-explorer');

console.log(`in wrapper demo.js file`);
console.log(`__filename in wrappper demo: ${__filename}`);
console.log(`__dirname in wrappper demo: ${__dirname}`);

wrapperExplorer.greet('John Doe');